{
	"private": true,
	"workspaces": {
		"packages": [
			"packages/*",
			"examples/*"
		],
		"catalog": {
			"typescript": "~5.8.3"
		}
	},
	"scripts": {
		"e2e": "maestro test e2e/maestro/flows",
		"clean": "lerna run clean && rm -rf node_modules && rm -rf bun.lockb",
		"build": "lerna run prepack",
		"publish": "lerna publish from-package",
		"release": "bun run build && bun run publish"
	},
	"devDependencies": {
		"@biomejs/biome": "2.1.2",
		"@lerna-lite/cli": "^4.7.0",
		"@lerna-lite/publish": "^4.7.0",
		"@lerna-lite/run": "^4.7.0",
		"@types/bun": "^1.2.19"
	}
}
