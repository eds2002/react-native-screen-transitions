import { AppleMusicTabPlaceholder } from "../tab-placeholder";

export default function SharedAppleMusicNew() {
	return (
		<AppleMusicTabPlaceholder
			tabLabel="New"
			body="The original expo-router example left this tab effectively empty. It stays here so the restored deprecated preset flow keeps the same nested tab structure."
		/>
	);
}
