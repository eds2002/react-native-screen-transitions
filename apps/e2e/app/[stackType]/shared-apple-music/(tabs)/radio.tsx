import { AppleMusicTabPlaceholder } from "../tab-placeholder";

export default function SharedAppleMusicRadio() {
	return (
		<AppleMusicTabPlaceholder
			tabLabel="Radio"
			body="Kept intentionally lightweight, matching the older demo where the compatibility question was about the SharedAppleMusic transition rather than radio-specific content."
		/>
	);
}
