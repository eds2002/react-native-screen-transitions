import { AppleMusicTabPlaceholder } from "../tab-placeholder";

export default function SharedAppleMusicLibrary() {
	return (
		<AppleMusicTabPlaceholder
			tabLabel="Library"
			body="This preserves the tab shell from the older example so you can launch the deprecated preset from within a nested navigator structure."
		/>
	);
}
