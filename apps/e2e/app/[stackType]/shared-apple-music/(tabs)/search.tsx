import { AppleMusicTabPlaceholder } from "../tab-placeholder";

export default function SharedAppleMusicSearch() {
	return (
		<AppleMusicTabPlaceholder
			tabLabel="Search"
			body="Search is still a placeholder here. The point of the restoration is verifying the old preset path visually, not building out a new Apple Music clone."
		/>
	);
}
