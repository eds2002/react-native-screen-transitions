# Docs App

This is the Docusaurus app for the `react-native-screen-transitions` documentation site.

## Scripts

```bash
bun start
bun run api:generate
bun run version:docs 3.x
bun run build
```

The site is deployed to GitHub Pages from the repository root workflow.
