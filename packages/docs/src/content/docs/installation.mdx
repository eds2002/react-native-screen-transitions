---
title: "Installation"
pageTitle: "Installation"
summary: "Install Screen Transitions and the runtime used by the current release."
description: "Install Screen Transitions with React 19.2, Reanimated 4, Worklets, and its navigation peers."
eyebrow: "Get Started"
group: "Get Started"
order: 2
to: /installation
---

## Requirements

The current release requires:

- React 19.2 or later
- Reanimated 4
- Worklets 0.8
- React Navigation 7.3 or later
- Gesture Handler 2.16.1 or later

Expo Router support currently requires Expo Router 56.2.10 or later and is marked Alpha. See [Expo Router](/expo-router) before using it in production.

## Install the package

<CodeBlock
  variant="terminal"
  defaultTab="npm"
  syncKey="package-manager"
  tabs={[
    { label: "npm", value: "npm", code: `npm install react-native-screen-transitions` },
    { label: "yarn", value: "yarn", code: `yarn add react-native-screen-transitions` },
    { label: "pnpm", value: "pnpm", code: `pnpm add react-native-screen-transitions` },
    { label: "bun", value: "bun", code: `bun add react-native-screen-transitions` },
  ]}
/>

## Install the animation runtime

Expo projects should let Expo choose compatible native versions:

```bash
npx expo install react-native-reanimated react-native-worklets \
  react-native-gesture-handler react-native-safe-area-context
```

For bare React Native projects:

<CodeBlock
  variant="terminal"
  defaultTab="npm"
  syncKey="package-manager"
  tabs={[
    {
      label: "npm",
      value: "npm",
      code: `npm install react-native-reanimated@^4 react-native-worklets@~0.8 \
  react-native-gesture-handler react-native-safe-area-context`,
    },
    {
      label: "yarn",
      value: "yarn",
      code: `yarn add react-native-reanimated@^4 react-native-worklets@~0.8 \
  react-native-gesture-handler react-native-safe-area-context`,
    },
    {
      label: "pnpm",
      value: "pnpm",
      code: `pnpm add react-native-reanimated@^4 react-native-worklets@~0.8 \
  react-native-gesture-handler react-native-safe-area-context`,
    },
    {
      label: "bun",
      value: "bun",
      code: `bun add react-native-reanimated@^4 react-native-worklets@~0.8 \
  react-native-gesture-handler react-native-safe-area-context`,
    },
  ]}
/>

Follow the Reanimated and Gesture Handler setup instructions for your React Native version. Bare iOS projects must also install pods after changing native dependencies.

## Choose a navigation host

Install `@react-navigation/native` for a React Navigation app:

```bash
npm install @react-navigation/native
```

Install Expo Router through Expo if your app uses file-based routing:

```bash
npx expo install expo-router
```

Continue with [React Navigation](/react-navigation) or the Alpha [Expo Router integration](/expo-router).

## Optional packages

Install `@react-native-masked-view/masked-view` when using navigation masks or library-managed zoom masking:

```bash
npm install @react-native-masked-view/masked-view
```

Boundary teleportation uses `react-native-teleport`:

```bash
npm install react-native-teleport
```
