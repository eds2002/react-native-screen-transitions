---
title: "Installation"
pageTitle: "Installation"
summary: "Install Screen Transitions and its peer dependencies."
description: "Install Screen Transitions with its required React Native, animation, gesture, and navigation peers."
eyebrow: "Get Started"
group: "Get Started"
order: 2
to: /installation
---

## Requirements

The current release requires:

- React 19.2 or later
- Reanimated 4
- Worklets 0.8 or later
- React Navigation 7.3 or later
- Gesture Handler 2.16.1 or later

Expo Router support currently requires Expo Router 56.2.10 or later and is marked Alpha. See [Expo Router](/expo-router) before using it in production.

## Install the package

<CodeBlock
  variant="terminal"
  defaultTab="npm"
  syncKey="package-manager"
  tabs={[
    { label: "npm", value: "npm", code: `npm install react-native-screen-transitions` },
    { label: "yarn", value: "yarn", code: `yarn add react-native-screen-transitions` },
    { label: "pnpm", value: "pnpm", code: `pnpm add react-native-screen-transitions` },
    { label: "bun", value: "bun", code: `bun add react-native-screen-transitions` },
  ]}
/>

## Install peer dependencies

For Expo projects:

```bash
npx expo install @react-navigation/native react-native-gesture-handler \
  react-native-reanimated react-native-safe-area-context react-native-worklets
```

For bare React Native projects:

<CodeBlock
  variant="terminal"
  defaultTab="npm"
  syncKey="package-manager"
  tabs={[
    {
      label: "npm",
      value: "npm",
      code: `npm install @react-navigation/native react-native-gesture-handler \
  react-native-reanimated react-native-safe-area-context react-native-worklets`,
    },
    {
      label: "yarn",
      value: "yarn",
      code: `yarn add @react-navigation/native react-native-gesture-handler \
  react-native-reanimated react-native-safe-area-context react-native-worklets`,
    },
    {
      label: "pnpm",
      value: "pnpm",
      code: `pnpm add @react-navigation/native react-native-gesture-handler \
  react-native-reanimated react-native-safe-area-context react-native-worklets`,
    },
    {
      label: "bun",
      value: "bun",
      code: `bun add @react-navigation/native react-native-gesture-handler \
  react-native-reanimated react-native-safe-area-context react-native-worklets`,
    },
  ]}
/>

Follow the Reanimated and Gesture Handler setup instructions for your React Native version. Bare iOS projects must also install pods after changing native dependencies. Expo Router apps already install Expo Router as part of their app setup.

Continue with [React Navigation](/react-navigation) or the Alpha [Expo Router integration](/expo-router).

## Optional packages

Install `@react-native-masked-view/masked-view` when using navigation masks or library-managed zoom masking:

```bash
npm install @react-native-masked-view/masked-view
```

Boundary teleportation uses `react-native-teleport`:

```bash
npm install react-native-teleport@1.2.0
```
