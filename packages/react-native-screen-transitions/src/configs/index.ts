import {
	DraggableCard,
	ElasticCard,
	SharedMaskedView,
	SlideFromBottom,
	SlideFromTop,
	ZoomIn,
} from "./presets";
import { DefaultSpec } from "./specs";

export const specs = {
	DefaultSpec,
};

export const presets = {
	SlideFromTop,
	ZoomIn,
	SlideFromBottom,
	DraggableCard,
	ElasticCard,
	SharedMaskedView,
};
