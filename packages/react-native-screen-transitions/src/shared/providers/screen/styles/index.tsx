export {
	useSlotLayoutStyles,
	useSlotProps,
	useSlotStackingStyles,
	useSlotStyles,
} from "./hooks/slot-resolvers";

export {
	ScreenSlotContext,
	type ScreenSlotName,
	ScreenSlotProvider,
	useScreenSlots,
} from "./slot.provider";
