export {
	useComposedSlotStyles,
	useSlotLayoutStyles,
	useSlotProps,
	useSlotStackingStyles,
	useSlotStyles,
} from "./hooks/slot-resolvers";

export {
	type ScreenSlotName,
	ScreenSlotProvider,
	useScreenSlots,
} from "./slot.provider";
