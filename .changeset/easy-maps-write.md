---
"react-native-screen-transitions": patch
---

Prevent bounds from measuring again when the screen is blurred, prevent running the beforeRemove listener on screens that don't have transitions enabled.
