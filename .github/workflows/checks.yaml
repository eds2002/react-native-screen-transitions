name: Checks
on:
  pull_request:
    types: [opened, synchronize]
  push:
    branches: [main]

jobs:
  checks:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - uses: oven-sh/setup-bun@v2
        with:
          bun-version: 1.2.20

      - name: Install dependencies
        run: bun install --frozen-lockfile

      - name: Lint
        run: bun run lint

      - name: Typecheck
        run: bun run typecheck

      - name: Build packages
        run: bun run build

      - name: Run tests
        run: bun test

      - name: Generate API docs
        run: bun run docs:api

      - name: Ensure generated API docs are committed
        run: git diff --exit-code -- apps/docs/docs/api/reference

      - name: Build docs
        run: bun run docs:build
